# macOS
.DS_Store
.AppleDouble
.LSOverride

# Archives
*.zip
*.gz
*.tar
# ...except the gzipped redraft corpus, which IS tracked (nothing in the app reads
# it, so it is stored compressed to stay well under GitHub's file-size limit).
!data/redraft_strategy_corpus.json.gz

# Local scratch folders
Vault/
Design/
Parlay API/
betting/
lineup_command/
Vault Fantasy Logo/

# Python build artifacts (scripts/ pipeline)
__pycache__/
*.py[cod]

# Misc
*.log
