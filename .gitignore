# macOS
.DS_Store
.AppleDouble
.LSOverride

# Archives
*.zip
*.gz
*.tar
# ...except the gzipped build corpora, which ARE tracked (nothing in the app reads
# them, so they are stored compressed to stay well under GitHub's file-size limit).
!data/redraft_strategy_corpus.json.gz
!data/sleeper_adp_corpus.json.gz
!data/sleeper_trade_corpus.json.gz

# Local scratch folders
Vault/
Design/
Parlay API/
betting/
lineup_command/
Vault Fantasy Logo/

# Python build artifacts (scripts/ pipeline)
__pycache__/
*.py[cod]

# Misc
*.log

# Claude Code local tooling (session data / downloaded skills / lockfile)
.claude/
skills-lock.json

# Redraft Strategy pipeline artifacts — regenerable by scripts/backfill_redraft_picks.py.
# The app reads the small derived JSONs (by_slot / curves / openings / …), never these:
# the 30M output corpus and its resumable checkpoint.
data/redraft_strategy_picks.json
data/redraft_strategy_picks_state.json
